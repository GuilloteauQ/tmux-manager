session_1:
    windows:
        - name: monitoring
          root: $HOME
          panes:
              command: htop
              right:
                  command: vim /tmp/log.log
session_2:
    windows:
        - name: foo
          root: /tmp
          panes:
              command:
        - name: bar
          root: $HOME
          panes:
              command: htop
        - name: baz
          root: $HOME
          panes:
              command: vim
              right:
                  command: nano
                  above:
                      command: htop
              below:
                  command:
